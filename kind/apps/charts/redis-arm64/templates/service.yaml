apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.fullname" . }}
  labels: 
    app: redis
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app: redis
